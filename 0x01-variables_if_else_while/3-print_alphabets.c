#include<stdio.h>

/**
 *main - prints the alphabet in lowercase, and then in uppercase.
 *Return: (0)
 */
int main(void)
{
char x;
for (x = 'a'; a <= 'z'; a++)
{
	putchar(x);
}
for (x = 'A'; a <= 'Z'; a++)
{
	putchar(x);
}
putchar('\n');
return (0);
}
